@model ParlorPrediction.Mvc.Models.Session.RegisterViewModel
@{
    ViewData["Title"] = "Register";
}

<section class="auth-shell pp-auth-shell pp-auth-shell--wide">
    <div class="auth-card auth-card--wide">
        <div class="pp-auth-brand">
            <span class="pp-auth-brand__mark">PP</span>
            <div class="pp-auth-brand__copy">
                <strong>Parlor Prediction</strong>
                <small>Team registration</small>
            </div>
        </div>

        <div class="section-heading">
            <span class="eyebrow">Parlor Prep Access</span>
            <h1>Create your account</h1>
            <p>Register first, confirm your email, and then wait for an Admin or Manager to approve your kitchen role.</p>
        </div>

        <form asp-action="Register" asp-controller="Session" method="post" class="row g-3">
            @Html.AntiForgeryToken()

            <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>

            <div class="col-md-6">
                <label asp-for="FirstName" class="form-label"></label>
                <input asp-for="FirstName" class="form-control" placeholder="First name" />
            </div>

            <div class="col-md-6">
                <label asp-for="LastName" class="form-label"></label>
                <input asp-for="LastName" class="form-control" placeholder="Last name" />
            </div>

            <div class="col-12">
                <label asp-for="Email" class="form-label"></label>
                <input asp-for="Email" class="form-control" placeholder="name@example.com" />
            </div>

            <div class="col-12">
                <label asp-for="PhoneNumber" class="form-label"></label>
                <input asp-for="PhoneNumber" class="form-control" placeholder="Phone number" />
            </div>

            <div class="col-md-6">
                <label asp-for="Password" class="form-label"></label>
                <input asp-for="Password" class="form-control" placeholder="Password" />
            </div>

            <div class="col-md-6">
                <label asp-for="PasswordConfirmation" class="form-label"></label>
                <input asp-for="PasswordConfirmation" class="form-control" placeholder="Confirm password" />
            </div>

            <div class="col-12 d-grid gap-2">
                <button type="submit" class="btn btn-primary btn-lg">
                    <i class="fa-solid fa-user-plus me-2"></i>
                    Register
                </button>
                <a class="btn btn-outline-secondary" asp-action="Login" asp-controller="Session">Back to Sign In</a>
            </div>
        </form>
    </div>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
